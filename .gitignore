# -----------------------------------------------------------------------------
# Eco Server – .gitignore (runtime, secrets, binaries, staging)
# -----------------------------------------------------------------------------

# Secrets (never commit)
.env
**/.env

# Runtime / generated data
Storage/
Logs/
Sentry/

# Binaries and downloaded game content (re-obtain via SteamCMD / scripts)
EcoServer
EcoServer.exe
linux64/
steamapps/
steamclient.so
steamclient64.dll
tier0_s64.dll
vstdlib_s64.dll
steam_appid.txt
WebClient/

# World preview image (regenerated by server)
Biomes.png

# Staging and download caches (re-run scripts to regenerate)
dev-files/eco-server-download/
dev-files/eco-server-linux/
dev-files/V1/

# OS and editor
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# Optional: uncomment to avoid tracking game __core__ (large; re-download with server)
Mods/__core__/
