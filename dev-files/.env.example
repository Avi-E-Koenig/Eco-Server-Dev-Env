# Server auth: ECO_TOKEN is passed as -userToken to EcoServer (Strange Cloud).
# Get a token: https://play.eco/en/account → Server Authentication → Generate Token

ECO_TOKEN=YourEcoToken

# Optional
# VERSION_BRANCH=public
# UPDATE_SERVER=false
# STEAM_FEEDBACK=false
